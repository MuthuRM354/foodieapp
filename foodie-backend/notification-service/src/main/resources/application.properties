# Application configuration
spring.application.name=notification-service
server.port=8084
server.servlet.context-path=/notification-service

# MongoDB Configuration
spring.data.mongodb.host=localhost
spring.data.mongodb.port=27017
spring.data.mongodb.database=foodie_notifications
spring.data.mongodb.auto-index-creation=true
spring.data.mongodb.username=admin
spring.data.mongodb.password=admin123

# Email configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:muthurm527@gmail.com}
spring.mail.password=${MAIL_PASSWORD:gvfa ctot nrlb ibki}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.connectiontimeout=5000

# Service URLs - for microservice communication
user.service.url=http://localhost:8081/user-service
order.service.url=http://localhost:8083/order-service
restaurant.service.url=http://localhost:8082/restaurant-service
payment.service.url=http://localhost:8085/payment-service

# Application-specific settings
app.email.enabled=true
app.frontend.url=http://localhost:3000

# CORS Configuration
app.cors.allowed-origins=http://localhost:3000
app.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS,PATCH
app.cors.max-age=3600

# Logging Configuration
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n
logging.level.com.foodieapp.notification=DEBUG
logging.level.org.springframework.web=INFO
logging.level.org.springframework.data.mongodb=INFO
logging.level.org.springframework.mail=DEBUG

# Swagger/OpenAPI
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html

# Security Configuration - Public paths
app.security.public-paths=/api/v1/notifications/**,/swagger-ui/**,/v3/api-docs/**

# Management endpoints for health checks
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always

# Error handling settings
server.error.whitelabel.enabled=false
server.error.include-stacktrace=never
server.error.include-message=always

# Timeout settings for rest template
app.rest-template.connect-timeout=5000
app.rest-template.read-timeout=8000

# Add the SMS API configuration
app.sms.api.url=https://textbelt.com/text
app.sms.api.key=textbelt